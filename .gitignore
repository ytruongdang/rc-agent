# Gradle / build
.gradle/
.kotlin/
**/build/
local.properties
.cxx/

# IDE
*.iml
.idea/
captures/

# OS
.DS_Store

# Artifacts
*.apk
*.aab

# Signing — never commit. assembleRelease fails without these, by design.
keystore/*.jks
keystore/*.keystore
keystore/*.p12
keystore/keystore.properties

# Samsung Knox SDK: compileOnly, supplied locally, not redistributable
app/libs/knoxsdk.jar

# Agent scratch
.claude/
